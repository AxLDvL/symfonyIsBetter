{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ animal.nom }}</td>
            </tr>
            <tr>
                <th>TailleMoyenne</th>
                <td>{{ animal.tailleMoyenne }}</td>
            </tr>
            <tr>
                <th>DureeDeVieMoyenne</th>
                <td>{{ animal.dureeDeVieMoyenne }}</td>
            </tr>
            <tr>
                <th>ArtMartial</th>
                <td>{{ animal.artMartial }}</td>
            </tr>
            <tr>
                <th>NumeroTelephone</th>
                <td>{{ animal.numeroTelephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_index') }}">back to list</a>

    <a href="{{ path('app_animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('animal/_delete_form.html.twig') }}
{% endblock %}
